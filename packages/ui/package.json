{
  "name": "ui",
  "version": "0.1.0",
  "description": "Jet-Black component lib",
  "type": "module",
  "module": "index.ts",
  "svelte": "index.ts",
  "scripts": {
    "lint": "prettier --plugin-search-dir=../prettier-config-custom --check  . && eslint .",
    "format": "prettier --plugin-search-dir=../prettier-config-custom --write ."
  },
  "devDependencies": {
    "eslint-config-custom": "workspace:*",
    "types": "workspace:*",
    "svelte": "^3.53.1"
  }
}
